{% extends 'base.html.twig' %}

{% block title %}My new Blog!{% endblock %}

{% block body %}
    {% for post in posts %}
        <h2>{{ post.title }}</h2> <h4>by {{ post.author.name }}</h4>
    {% endfor %}
{% endblock %}